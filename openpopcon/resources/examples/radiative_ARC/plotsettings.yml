#-----------------------------------------------------------------------
# OpenPOPCON Plotting Settings: radiative ARC
#
# Only the entries that differ from resources/default_plotsettings.yml are
# listed; anything left out falls back to the default.
#
# The contours are the defaults; only the axes change, to on-axis values.
#-----------------------------------------------------------------------

yax: "n20_ax" # nG, n20_av or n20_ax
xax: "T_i_ax" # T_i_av, T_i_ax, T_e_av or T_e_ax; either axis takes either family
figsize: [8,6]
fill_invalid: True

plotoptions: {}
