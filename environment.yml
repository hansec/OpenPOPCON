# run: conda env create --file environment.yml
name: openPOPCON
dependencies:
- python=3.10
- anaconda
- pip
- numpy
- scipy
- matplotlib
- numba
- pyyaml
- netcdf4  # For VMEC file reading
pip:
  # No longer need t3d dependency - VMEC reading capability integrated directly